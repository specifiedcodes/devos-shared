/** Story Kanban board status */
export enum StoryStatus {
  BACKLOG = 'backlog',
  IN_PROGRESS = 'in_progress',
  REVIEW = 'review',
  DONE = 'done',
}

/** Story priority levels */
export enum StoryPriority {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
}
